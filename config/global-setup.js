module.exports = async () => {
    console.log('Global setup');
    /* Example global setup code, such as environment setup or other tasks. Use this for one-time setup before execution like DB Connection and Environment variable
    Update the below URL for different URL*/
    //process.env.BASE_URL = 'https://localhost:9393/';
  };